{
  "slug": "cloudflare-rate-limit",
  "title": "Rate-limited API proxy at the edge",
  "description": "Use Workers KV and Durable Objects to throttle requests before they reach your origin.",
  "category": "Cloudflare Workers",
  "difficulty": "Advanced",
  "readingTime": "18 min",
  "featured": true,
  "tags": [
    "Cloudflare Workers",
    "Edge",
    "APIs"
  ],
  "tools": [
    "Cloudflare Workers",
    "Workers KV",
    "Durable Objects"
  ],
  "cover": "/images/workflows/cloudflare-rate-limit.webp",
  "overview": "A Cloudflare Worker that rate-limits incoming API requests per client using a Durable Object for strict, race-free enforcement on hot endpoints, with Workers KV available for longer-lived counters.",
  "prerequisites": [
    "A Cloudflare account with Workers enabled",
    "Wrangler CLI installed and authenticated",
    "A KV namespace and Durable Object binding configured in wrangler.toml"
  ],
  "steps": [
    {
      "title": "Create the Worker project",
      "content": "Scaffold a new Worker with Wrangler.",
      "code": {
        "language": "bash",
        "code": "npm create cloudflare@latest rate-limit-proxy\ncd rate-limit-proxy"
      }
    },
    {
      "title": "Define the Durable Object",
      "content": "Each client gets its own Durable Object instance that tracks a sliding window of request timestamps in memory.",
      "code": {
        "language": "javascript",
        "code": "export class RateLimiter {\n  constructor(state) {\n    this.state = state;\n    this.requests = [];\n  }\n\n  async fetch(request) {\n    const now = Date.now();\n    this.requests = this.requests.filter(t => now - t < 60_000);\n    if (this.requests.length >= 60) {\n      return new Response('Rate limit exceeded', { status: 429 });\n    }\n    this.requests.push(now);\n    return new Response('ok');\n  }\n}"
      }
    },
    {
      "title": "Route requests through the limiter",
      "content": "In the main Worker, derive a Durable Object ID from the client's IP or API key, then forward the check before proxying to the origin.",
      "code": {
        "language": "javascript",
        "code": "export default {\n  async fetch(request, env) {\n    const ip = request.headers.get('CF-Connecting-IP');\n    const id = env.RATE_LIMITER.idFromName(ip);\n    const stub = env.RATE_LIMITER.get(id);\n    const check = await stub.fetch(request);\n\n    if (check.status === 429) return check;\n    return fetch(request);\n  }\n};"
      }
    },
    {
      "title": "Bind the Durable Object",
      "content": "Declare the class and a KV namespace for longer-lived counters in wrangler.toml.",
      "code": {
        "language": "toml",
        "code": "[[durable_objects.bindings]]\nname = \"RATE_LIMITER\"\nclass_name = \"RateLimiter\"\n\n[[kv_namespaces]]\nbinding = \"RATE_KV\"\nid = \"...\""
      }
    },
    {
      "title": "Deploy and test",
      "content": "Publish the Worker and send requests past the threshold to confirm it returns a 429 once the limit is hit.",
      "code": {
        "language": "bash",
        "code": "npx wrangler deploy"
      }
    }
  ],
  "architecture": "Each client is mapped to a single Durable Object instance by IP or API key, giving strictly consistent, race-free counting even under concurrent requests at the edge, something KV alone can't guarantee due to eventual consistency. The Worker checks the limiter before proxying to origin, so blocked requests never leave Cloudflare's network.",
  "result": "An edge-deployed proxy that enforces a 60-requests-per-minute limit per client with sub-millisecond overhead, protecting the origin from abuse without adding a centralized rate-limiting service."
}